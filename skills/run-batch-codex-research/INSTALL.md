# run-batch-codex-research

Migrating from retired run-batch-codex-research to run-codex-2 batch mode for codex template fanout.

**Category:** orchestration

## Install

Install the active replacement:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur/skills/run-codex-2
```

Or install the full pack:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur
```

This old skill path is a compatibility redirect only. Use `run-codex-2` batch mode for template-by-input codex fanout.
