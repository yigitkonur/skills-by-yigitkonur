# build-agent-cli

Building CLI tools that AI agents can reliably invoke, parse, and compose — structured output, exit codes, non-interactive modes, idempotency, streaming, and MCP vs CLI decisions.

**Category:** development

## Install

Install this skill individually:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur/skills/build-agent-cli
```

Or install the full pack:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur
```
