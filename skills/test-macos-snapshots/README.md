# test-macos-snapshots

Validating a macOS app UI with screenshots, snapshot automation, or visual regression checks, with expectation-first evidence and drift analysis.

**Category:** testing

## Install

Install this skill individually:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur/skills/test-macos-snapshots
```

Or install the full pack:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur
```
