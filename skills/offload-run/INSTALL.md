# offload-run

running a project's build/test/install commands in a remote cloud sandbox instead of on your own machine — npm/pnpm, vitest, vite/next, tsc, eslint, pytest, or macOS/Xcode builds.

**Category:** development

## Install

**As a plugin (easy install / uninstall via `/plugin`):**

```
/plugin marketplace add yigitkonur/skills-by-yigitkonur
/plugin install offload-run@yigitkonur
```

**Or with the `skills` CLI — this skill only:**

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur/skills/offload-run
```

**Or the full pack:**

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur
```
