# build-licenseseat-swift

Integrating the LicenseSeat Swift SDK into a macOS/Swift app — activation, validation, offline grace, entitlements, seat management — with source-verified API surface and adversarially-hardened integration blueprint.

**Category:** development

## Install

**As a plugin (easy install / uninstall via `/plugin`):**

```
/plugin marketplace add yigitkonur/skills-by-yigitkonur
/plugin install build-licenseseat-swift@yigitkonur
```

**Or with the `skills` CLI — this skill only:**

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur/skills/build-licenseseat-swift
```

**Or the full pack:**

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur
```
