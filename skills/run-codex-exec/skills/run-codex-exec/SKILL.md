---
name: run-codex-exec
description: Use skill if you are following the deprecated codex exec orchestration install path and need the current orchestrate-codex exec mode.
---

# run-codex-exec

This is a deprecated compatibility shim. Do not restore the old implementation here.

Use `orchestrate-codex` instead:

```bash
node skills/orchestrate-codex/skills/orchestrate-codex/scripts/orchestrate-codex.mjs exec --tasks <tasks.json>
```

Read `skills/orchestrate-codex/skills/orchestrate-codex/SKILL.md` for the active runtime contract.
