# swift-quality-hooks

Installing or configuring a Swift pre-commit hook with SwiftLint and SwiftFormat on a macOS, iOS, tvOS, watchOS, or visionOS project.

**Category:** development

## Install

Install this skill individually:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur/skills/swift-quality-hooks
```

Or install the full pack:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur
```
