# audit-completion

Audit what's actually done vs. claimed done — classify every task by a 22-status taxonomy, then execute until every in-scope item reaches Implemented.

**Category:** productivity

## Install

Install this skill individually:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur/skills/audit-completion
```

Or install the full pack:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur
```
